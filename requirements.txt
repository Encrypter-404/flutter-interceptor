frida==17.9.10
pywebview==6.2.1
cryptography>=42.0
brotli>=1.1.0          # decode br-encoded response bodies in the Requests tab (optional but common)
h2>=4.1.0             # HTTP/2 + gRPC capture in the in-tool proxy (optional; falls back to HTTP/1.1)
